package com.taskmanagement.models;

/**
 * Enum representing the priority levels for tasks.
 * Ensures type safety by restricting priority values to predefined constants.
 */
public enum Priority {
    LOW,
    MEDIUM,
    HIGH
}